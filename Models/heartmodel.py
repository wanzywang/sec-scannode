# coding=utf-8


class HeartModel:
    def __init__(self, server, timespan, active, info):
        self.server = server
        self.timespan = timespan
        self.active = active
        self.info = info
